# Pro-Visualize runtime environment — copy and fill in before `make run`
# This file is committed with empty/safe defaults. Override locally as needed.

# Override the default Streamlit upload limit (MB)
STREAMLIT_SERVER_MAX_UPLOAD_SIZE=500
