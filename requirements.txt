# pro-visualize/requirements.txt

# --- Core Data Science & Math ---
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1

# --- Visualization ---
plotly==5.22.0
matplotlib==3.9.0
seaborn==0.13.2
upsetplot==0.9.0
pyvis==0.3.2
venn==0.1.3
# Static image export from Plotly figures (PNG/SVG via fig.to_image()).
# Bundles a headless Chromium build; in Docker we install the prerequisite
# system libs (libgbm1, libasound2, libxshmfence1) — see Dockerfile (P7).
kaleido==0.2.1
# HTML report templating (P1). Streamlit pulls jinja2 transitively, but we
# pin explicitly so the report builder has a guaranteed contract.
jinja2==3.1.6

# --- Machine Learning & Clustering ---
scikit-learn==1.5.0
umap-learn==0.5.6

# --- Bioinformatics & API ---
gprofiler-official==1.0.0
requests==2.32.3
networkx==3.3

# --- Single-Cell Proteomics (SCP) ---
# Core single-cell framework (AnnData, scanpy algorithms).
# Pinned exactly: AnnData on-disk/in-memory format changes between minor
# versions, so reproducibility requires an exact scanpy/anndata pair.
scanpy==1.11.5
anndata==0.12.11
# Batch correction for SCP integration (modules/scp_module.py "Run Harmony").
# The import is guarded (HARMONY_AVAILABLE) so the app still runs if removed.
harmonypy==2.0.0
# Gene-set enrichment (used in GSEA / Enrichr blocks)
gseapy==1.2.1
# Leiden clustering (required for sc.tl.leiden)
igraph==1.0.0
leidenalg==0.11.0

# --- Web Application Framework ---
streamlit==1.36.0