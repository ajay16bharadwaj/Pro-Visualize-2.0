services:
  pro-visualize:
    build: .
    ports:
      - "${PORT:-8501}:8501"
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
    env_file:
      - .env.docker
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
